class Vehicle {
    // method
    drive(): void {
    console.log('chugga chugga');
  }
}

// create new keyword vehicle with class Vehicle
const vehicle = new Vehicle();
// access method
vehicle.drive();