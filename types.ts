// type inference in action
const carMakers = ['ford', 'toyota', 'chevy'];

// new date object instances
const dates = [new Date(), new Date()];

// array type string with square bracket
const carsByMake: string[][] = [];